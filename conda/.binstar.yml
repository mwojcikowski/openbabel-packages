package: openbabel

build_targets: conda

platform:
  - linux-64

engine:
  - python=2
  - python=3


before_script:
  - conda update --all --yes
  - conda config --add channels mwojcikowski
  
script: conda build .
